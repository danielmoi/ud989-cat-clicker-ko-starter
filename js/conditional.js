var age = 18;

// Using 'conditional' operator
var name = (age > 10)  ? "Old" : "Young";

name;

// Using 'if' and 'else'
if (age > 10) {
  name = "Old";
}

else {
  name = "Young";
}

// ************
